<head>		not displayed, backend(or title of page)
<body>		displayed in page

<h1 to h6> 	header level 1 to 6

<b>             "black" negrito
<i>             "italic" itálico
<a href="">     link

<form>
<input>

<p>             paragraph
<ol>            ordered list (display counters)
<ul> 		    unordered list (doesn't count)
<li> 		    list item
<br>            break line
<img>           images(from jpg, png, to gifs)
<figcaption>    in a <figure> section, should add some additional information about an image

tables:
<table></table>: should open a table element(tabela em portugues)
<tr>            table row
<th>            table head(to start the table with some initial values); some attributes: colspan(?),
<td>            table data(for the data)
<thead>
<tbody>


semanthic tags(sections, others):::
<section>       default section
<header>        should be the first section of a page
<main>          one per page, should be the main area of a page
<nav>           links para navegar entre áreas do website. aria-label="primary-navigation"
<figure>        to use an image, <figcaption>, etc.
<footer>        one per page, should be the final area of the page