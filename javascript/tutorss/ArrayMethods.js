myStrToSplit = 'ab3'
mySequence = myStrToSplit.split("") //split to an array, arg is from which element it'll split
mySequence.reverse() //reverse order in array
myStr = mySequence.join(", ") //same as python, for each iteration of array, add to var